# VTU-RANKERS
RouteMaster is a web app optimizing warehouse fulfillment. It ingests JSON data to map walkable paths, shelves, and targets. Using a shortest-path algorithm, it calculates the best orthogonal route to collect all items while avoiding obstacles. The UI visualizes the grid and outputs mandatory step telemetry .
